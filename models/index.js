
const {User, Rol} = require('./userData');
const {Category} = require('./categoryData');
const {Product} = require('./productData');
const Server = require('./server');


module.exports = {
    User,
    Rol,
    Category,
    Product,
    Server
};